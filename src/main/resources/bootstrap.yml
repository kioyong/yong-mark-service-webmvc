server:
  port: 443
  ssl:
    key-store: 'classpath:www.kioyong.xyz.jks'
    key-store-password: klljkljli@34
spring:
  application:
      name: mark
  data:
    mongodb:
      host: mongodb
      port: 27017
      database: mark
  profiles:
    active: dev
logging:
  file: application.log
  level:
    com.yong: DEBUG
    org.springframework.cloud.config.client: DEBUG

management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
yong:
  string: "from bootstrap.yml"

hibernate:
  transaction:
    jta:
      platform: