server:
  port: 9003

spring:
  application:
      name: mark
  data:
    mongodb:
      uri: mongodb://mongodb:27017/mark
      database: mark

logging:
  level:
    com.yong: DEBUG
    org.springframework.cloud.config.client: DEBUG

management:
  security:
    enabled: false

yong:
  string: "from bootstrap.yml"

