server:
  port: 9003
spring:
  application:
      name: mark
  data:
    mongodb:
      host: mongodb
      port: 27017
      database: mark
logging:
  file: application.log
  level:
    com.yong: DEBUG
    org.springframework.cloud.config.client: DEBUG

management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
yong:
  string: "from bootstrap.yml"

hibernate:
  transaction:
    jta:
      platform: