server:
  port: 9003
  servlet:
    context-path: /mark


spring:
  application:
      name: mark
  data:
    mongodb:
      uri: mongodb://127.0.0.1:27010/mark
      database: mark

logging:
  level:
    com.yong: DEBUG
    org.springframework.cloud.config.client: DEBUG

management:
  security:
    enabled: false

yong:
  string: "from bootstrap.yml"

